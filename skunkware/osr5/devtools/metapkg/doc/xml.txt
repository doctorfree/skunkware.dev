MetaPKG XML Backend Notes
=========================

The XML backend is a useful way of describing a distribution tree in a
format-neutral way. I have not (yet) defined a formal XML DTD for the
output, but it basically reflects the hierarchy as expressed in the tree
or via the control script.

