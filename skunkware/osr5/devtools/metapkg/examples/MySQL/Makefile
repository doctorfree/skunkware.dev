#
# Makefile for producing custom-installable images of the product
# `MySQL', version 4.1.10Na.
#
# Automatically generated by MetaPKG 2.0.4 (using CDMT backend 1.0.8)
#

PATH=/usr/bin:/bin:/usr/gnu/bin:/etc
all: cdmt.config
	CDMT_DIR=$${CDMT_DIR:-`pwd`} cdmtParse || exit 1
	CDMT_DIR=$${CDMT_DIR:-`pwd`} cdmtCompress || exit 1
	CDMT_DIR=$${CDMT_DIR:-`pwd`} cdmtArchive || exit 1
	rm -f archives/TAPE/*.files archives/TAPE/*.sum

clean:
	rm -fr archives sso

cdmt.config:
	if [ -f CDMT.MACROS -a ! -f cdmt.config ]; then \
		mv -f CDMT.MACROS cdmt.config; fi

