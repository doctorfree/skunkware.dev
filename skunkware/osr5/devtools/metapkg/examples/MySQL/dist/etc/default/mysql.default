#
# This file controls the startup of the MySQL database system.
#
# MYSQLUSER is the user ID to run the mysqld process as, and should
# be the owner of the database directory. Defaults to mysql if that
# user exists, nouser if not.
#
# MYSQLDATA is the location of the database. Defaults to /usr/mysql/data.
#
# MYSQLOPTS is a string contain startup options for mysqld.
#
# Please use mkdev mysql to configure MySQL
#
