
tapecp is a TCL script that reads and writes custom+ format tapes.
It will take a directory of files named VOL.000.000, VOL.001.000...
and write them all to tape.  It will also read a tape and create
those files.

bench.tar is a primitive CPU benchmark utility.

tcltrace.tar is a primitive branch coverage tool for TCL.
