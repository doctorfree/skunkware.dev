Install imapd in /etc/imapd
services must be appended to /etc/services
inetd.conf must be appended to /etc/inetd.conf

mmdf is default folder format, sendmail will be read but
new IMAP folders will be mmdf.

Lastly, kill -1 "inetd's pid", and imap is active

