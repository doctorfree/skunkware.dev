To build libpcap/tcpdump on UnixWare 1.1:

1. Edit mkdep in ecah directory and replace the cc -M line with the two lines 
above it.

2. cd libpcap-0.0 ; ./configure ; make

3. cd ../tcpdump-3.0; ./configure; make
