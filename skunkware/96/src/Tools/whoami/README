whoami: print user or group IDs.
Usage: whoami [huUgGns]
h: Print this help.
u: Print user ID.
U: Print effective user ID.
g: Print group ID.
G: Print effective group ID.
n: Switch to printing numeric IDs.
s: Switch back to printing string IDs.
If no arguments are given, the default of 'u' is used.
To print a numeric user ID, use 'nu'.
If the string form of an ID is not found, the numeric form is printed in
its place.
Example: to print each ID, with the numeric followed by the string form
printed for each:
whoami nusunUsUngsgnGsG
