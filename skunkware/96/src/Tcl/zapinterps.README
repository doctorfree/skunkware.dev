A combination of wish and C code to zap those pesky interpreter
names for wishes that died a nasty death.  The wish code tries to
figure out which interpreters no longer exist; the C code fixes the
X "resource" string that lists interpreter names.
