This is a file browsing tool with drag&drop capability.  It works with
tk3.6 and tcl7.3.  It requires the BLT extension.

To configure and install, run 
	wish -f tkfb.install

To test it out, run
	tkfb &
	printer lw111 &
	recycle &
	tkreify /tmp &

Acknowledgements:
install.tcl is by Brent Welch.
