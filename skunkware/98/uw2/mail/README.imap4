Install imapd as /etc/imapd, make sure execute permissions are there.
services must be appended to /etc/services
inetd.conf must be appended to /etc/imapd.conf

then kill -1 "inetd's pid", and imap is active
if that doesn't work then reboot.
